{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!
{% endblock %}


{% block body %}


	<style>
	</style>



<table class="table table-dark">
	<thead>
		<tr>
<th scope="col">Prénom</th>

<th scope="col">Nom</th>

<th scope="col">Rôle</th>

			<th scope="col">Date de sortie</th>
			<th scope="col">Profile</th>
{% if app.user.hasRole("ROLE_RH") %}

<th scope="col">Modifier</th>
<th scope="col">Supprimer</th>{% endif %}


			
		</tr>
	</thead>
{% for user in users %}

	<tbody>
		<tr>
<td>{{ user.username }}</td>
<td>{{ user.lastname }}</td>
<td>{{ user.secteur }}</td>
<td>
	{% if user.datesortie is not null %}
		{{ user.datesortie }}
	{% else %}
		La date de sortie n'est pas définie.
	{% endif %}
</td>
</td>
<td>
	<a href="{{ path('vue_user', {'id': user.id}) }}">Voir le profil</a>
</td>
{% if app.user.hasRole("ROLE_RH") %}

<td>
	<a href="{{ path('vue_user', {'id': user.id}) }}">Modifier</a>
</td>
<td>
<a href="{{ path('app_user_delete', {'id': user.id}) }}">supprimer</a>

</td>

{% endif %}


		</tr>

	</tbody>


	{% endfor %}
</table>

{% endblock %}

